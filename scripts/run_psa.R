# Enable PSA by setting psa.enabled: true in config/params.yaml, then run:
# targets::tar_make()  # PSA targets will execute
message("To run PSA, set psa.enabled: true in config/params.yaml and run targets::tar_make().")
